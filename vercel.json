{
  "framework": "vite",
  "installCommand": "cd hics-app && npm install",
  "buildCommand": "cd hics-app && npm run build",
  "outputDirectory": "hics-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
